{
  "config": {
    "step": {
      "user": {
        "title": "MG4 Home Assistant Bridge",
        "description": "Creates a device; the car app pushes telemetry via the mg4_bridge.push service from any network using your HA URL and token. Prefix must match the app entity prefix.",
        "data": {
          "name": "Device name",
          "prefix": "Entity prefix"
        }
      }
    },
    "error": {
      "invalid_prefix": "Invalid prefix"
    },
    "abort": {
      "already_configured": "This prefix is already configured."
    }
  }
}
